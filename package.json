{
  "name": "lumi-reading-ruler",
  "version": "1.1.6",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/frontend.ts --outfile dist/frontend.js --target browser",
    "test": "node tests/double-tap-collapse.test.mjs && node tests/offscreen-drawer-yield.test.mjs",
    "verify": "bun run build && bun run test"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "latest"
  }
}
